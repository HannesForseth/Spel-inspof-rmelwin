{
  "name": "skogens-skordare",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "serve -s dist -l ${PORT:-3000}"
  },
  "dependencies": {
    "three": "^0.169.0",
    "serve": "^14.2.4"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
